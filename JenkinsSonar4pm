pipeline {
    agent any

    stages {
        stage('Sonar Analysis') {
            steps {
                echo 'Testing..'
                sh 'cd webapp && sudo docker run  --rm -e SONAR_HOST_URL="http://54.215.59.160:9000" -e SONAR_LOGIN="sqp_ad9e85ee8c16e69b0032c5cb9e408f87d3247c59"  -v ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms'
            }
        }

        
    }
}
